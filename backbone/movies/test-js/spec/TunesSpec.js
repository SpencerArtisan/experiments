// Generated by CoffeeScript 1.3.3
var movieData;

movieData = [
  {
    "title": "Birdemic",
    "rating": 1.2
  }, {
    "title": "The Godfather",
    "rating": 9.2
  }
];

describe("Movie", function() {
  return it("creates from data", function() {
    this.movie = new Movie(movieData[1]);
    return expect(this.movie.get('title')).toEqual('The Godfather');
  });
});

describe("Movies", function() {
  return it('orders by rating, highest first', function() {
    this.movies = new Movies(movieData);
    expect(this.movies.models[0].get('title')).toEqual('The Godfather');
    return expect(this.movies.models[1].get('title')).toEqual('Birdemic');
  });
});
